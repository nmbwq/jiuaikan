package top.wzmyyj.zymk.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import top.wzmyyj.zymk.model.db.dao.SearchHistoryDb;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SEARCH_HISTORY_DB".
*/
public class SearchHistoryDbDao extends AbstractDao<SearchHistoryDb, Long> {

    public static final String TABLENAME = "SEARCH_HISTORY_DB";

    /**
     * Properties of entity SearchHistoryDb.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Search_word = new Property(1, String.class, "search_word", false, "SEARCH_WORD");
        public final static Property Search_time = new Property(2, long.class, "search_time", false, "SEARCH_TIME");
    }


    public SearchHistoryDbDao(DaoConfig config) {
        super(config);
    }
    
    public SearchHistoryDbDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SEARCH_HISTORY_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SEARCH_WORD\" TEXT NOT NULL UNIQUE ," + // 1: search_word
                "\"SEARCH_TIME\" INTEGER NOT NULL );"); // 2: search_time
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SEARCH_HISTORY_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SearchHistoryDb entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getSearch_word());
        stmt.bindLong(3, entity.getSearch_time());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SearchHistoryDb entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getSearch_word());
        stmt.bindLong(3, entity.getSearch_time());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SearchHistoryDb readEntity(Cursor cursor, int offset) {
        SearchHistoryDb entity = new SearchHistoryDb( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // search_word
            cursor.getLong(offset + 2) // search_time
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SearchHistoryDb entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSearch_word(cursor.getString(offset + 1));
        entity.setSearch_time(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SearchHistoryDb entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SearchHistoryDb entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SearchHistoryDb entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
